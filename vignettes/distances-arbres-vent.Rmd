---
title: "Distances aux Arbres et Effet du Vent"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Distances aux Arbres et Effet du Vent}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.width = 8,
  fig.height = 6,
  message = FALSE,
  warning = FALSE
)
```

```{r setup}
library(covariablechamps)
library(sf)
library(terra)
library(ggplot2)
```

## Introduction

Ce guide présente les fonctions du package `covariablechamps` pour calculer les distances aux arbres en tenant compte de la direction du vent. Ces covariables sont essentielles pour modéliser la protection des cultures contre le vent et l'ombrage.

Le package offre plusieurs approches:

- **Distance simple**: Distance euclidienne aux arbres les plus proches
- **Distances directionnelles (amont/aval)**: Distance distinguant le sens du vent
- **Fetch elliptique**: Distance avec effet de buffer elliptique aligné sur le vent

## Chargement du champ M2

Le package inclut un champ d'exemple (`M2`) situé au Québec.

```{r charger-champ}
champ <- st_read(system.file("extdata", "M2.shp", package = "covariablechamps"))

ggplot() +
  geom_sf(data = champ, fill = "lightgreen", alpha = 0.5) +
  theme_minimal() +
  labs(title = "Champ M2",
       subtitle = "Champ d'exemple inclus dans le package")
```

## Téléchargement des données LiDAR

Le MNT et MNE sont téléchargés depuis le DataCube du Canada.

```{r telecharger-lidar, cache = TRUE}
# Télécharger les données LiDAR
lidar <- telecharger_lidar(
  polygone = champ,
  mne = TRUE
)

mne <- lidar$mne
mnt <- lidar$mnt

plot(mne, main = "MNE (Modèle Numérique d'Élévation)",
     col = hcl.colors(100, "Terrain"))
plot(sf::st_geometry(champ), add = TRUE, border = "black", lwd = 2)
```

## Création d'arbres synthétiques

Pour la démonstration, nous créons des arbres synthétiques autour du champ. En pratique, les arbres proviendraient de `extraire_classifier_haies_lidar()`.

```{r creer-arbres}
# Obtenir l'étendue du champ
bbox_champ <- st_bbox(champ)

# Créer des arbres synthétique le long des bordures
set.seed(42)
n_arbres <- 50
coords_arbres <- rbind(
  # Arbres au nord
  cbind(runif(n_arbres/4, bbox_champ["xmin"], bbox_champ["xmax"]),
         runif(n_arbres/4, bbox_champ["ymax"], bbox_champ["ymax"] + 30)),
  # Arbres au sud
  cbind(runif(n_arbres/4, bbox_champ["xmin"], bbox_champ["xmax"]),
         runif(n_arbres/4, bbox_champ["ymin"] - 30, bbox_champ["ymin"])),
  # Arbres à l'ouest
  cbind(runif(n_arbres/4, bbox_champ["xmin"] - 30, bbox_champ["xmin"]),
         runif(n_arbres/4, bbox_champ["ymin"], bbox_champ["ymax"])),
  # Arbres à l'est
  cbind(runif(n_arbres/4, bbox_champ["xmax"], bbox_champ["xmax"] + 30),
         runif(n_arbres/4, bbox_champ["ymin"], bbox_champ["ymax"]))
)

# Créer l'objet sf
pts <- lapply(1:nrow(coords_arbres), function(i) {
  sf::st_point(coords_arbres[i, ])
})
arbres <- sf::st_sf(
  id = 1:nrow(coords_arbres),
  geometry = sf::st_sfc(pts, crs = st_crs(champ))
)

ggplot() +
  geom_sf(data = champ, fill = "lightgreen", alpha = 0.3) +
  geom_sf(data = arbres, color = "darkgreen", size = 2) +
  theme_minimal() +
  labs(title = "Champ M2 avec arbres synthétiques")
```

## Distance simple aux arbres

La fonction `calculer_distance_arbres()` calcule la distance euclidienne aux arbres.

```{r distance-simple, cache = TRUE}
dist_simple <- calculer_distance_arbres(
  arbres_sf = arbres,
  champ_bbox = champ,
  resolution = 2,
  buffer_arbre = 3,
  max_distance = 100
)

visualiser_distance_arbres(dist_simple, type = "buffer")
```

## Distances amont/aval avec direction du vent

La fonction `calculer_distances_amont_aval()` distingue les distances selon la direction du vent:

- **Amont**: Vent qui vient DE l'arbre (protection contre le vent)
- **Aval**: Vent qui va VERS l'arbre (accélération après l'arbre)

```{r distances-amont-aval, cache = TRUE}
dist_dir <- calculer_distances_amont_aval(
  arbres_sf = arbres,
  angle_vent = 270,
  champ_bbox = champ,
  resolution = 2,
  buffer_arbre = 3,
  angle_focal = 45,
  max_distance = 100,
  taille_lissage = 5
)

viz <- visualiser_distances_vent(dist_dir, type = "comparaison")
viz$amont
viz$aval
```

### Visualiser chaque type séparément

```{r visualisation-separee}
visualiser_distances_vent(dist_dir, type = "amont")
visualiser_distances_vent(dist_dir, type = "aval")
visualiser_distances_vent(dist_dir, type = "totale")
```

## Fetch de vent elliptique

La fonction `calculer_fetch_vent()` calcule un buffer elliptique aligné avec la direction du vent.

```{r fetch-elliptique, cache = TRUE}
fetch <- calculer_fetch_vent(
  arbres_sf = arbres,
  angle_vent = 270,
  champ_bbox = champ,
  resolution = 2,
  max_fetch = 100,
  coef_ellipse = 3
)

viz_fetch <- visualiser_fetch(fetch, type = "comparaison")
viz_fetch$simple
viz_fetch$elliptique
```

## Comparaison des méthodes

```{r comparaison}
par(mfrow = c(2, 2), mar = c(3, 3, 3, 3))

plot(dist_simple$distance_buffer, main = "Distance simple (buffer)",
     col = hcl.colors(100, "Terrain"))
plot(sf::st_geometry(champ), add = TRUE, border = "black", lwd = 2)

plot(dist_dir$amont, main = "Amont (m)",
     col = hcl.colors(100, "Blues"))
plot(sf::st_geometry(champ), add = TRUE, border = "black", lwd = 2)

plot(dist_dir$aval, main = "Aval (m)",
     col = hcl.colors(100, "Reds"))
plot(sf::st_geometry(champ), add = TRUE, border = "black", lwd = 2)

plot(dist_dir$totale, main = "Distance totale (m)",
     col = hcl.colors(100, "Viridis"))
plot(sf::st_geometry(champ), add = TRUE, border = "black", lwd = 2)
```

## Choisir la bonne approche

| Fonction | Usage | Avantage |
|----------|-------|----------|
| `calculer_distance_arbres()` | Distance simple sans direction | Rapide, bon pour ombrage |
| `calculer_distances_amont_aval()` | Distances avec effet tampon et lissage | Plus précis pour vent |
| `calculer_fetch_vent()` | Buffer elliptique pour effet directionnel | Modélisation physically-based |
| `calculer_distances_vent()` | Version simple des distances directionnelles | Bon compromis |

## Applications

- **Protection des cultures**: Identifier les zones protégées par les arbres
- **Modélisation du vent**: Estimer la vitesse du vent à chaque point
- **Planification**: Optimiser l'implantation de nouvelles haies
