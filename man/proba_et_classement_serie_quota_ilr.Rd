% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proba_serie.R
\name{proba_et_classement_serie_quota_ilr}
\alias{proba_et_classement_serie_quota_ilr}
\title{Calculer les probabilités et le classement des séries avec quota ILR}
\usage{
proba_et_classement_serie_quota_ilr(
  polygones_sf,
  table_series,
  mnt,
  id_col = "Code.polygone",
  serie_col = "Composante",
  poids_col = "Pourcentage",
  sable_col = "Sable",
  limon_col = "Limon",
  argile_col = "Argile",
  grid_m = 20,
  sigma_voisin = 60,
  alpha_voisin = 8,
  beta_mnt = 0.2,
  lambda_base = 0.25,
  base_eps = 1e-06,
  sim_scale = c("zscore", "none"),
  sim_clip = 3,
  tempdir = NULL,
  write_intermediate = TRUE,
  verbose = TRUE,
  progress = TRUE
)
}
\arguments{
\item{polygones_sf}{Un objet `sf` contenant les polygones (champs/parcelles)}

\item{table_series}{Un `data.frame` avec les informations de série par polygone}

\item{mnt}{Un objet `SpatRaster` (MNT/DEM)}

\item{id_col}{Nom de la colonne identifiant les polygones dans `table_series`}

\item{serie_col}{Nom de la colonne indiquant la série de sol}

\item{poids_col}{Nom de la colonne avec le poids/proportion de la série}

\item{sable_col}{Nom de la colonne avec le pourcentage de sable}

\item{limon_col}{Nom de la colonne avec le pourcentage de limon}

\item{argile_col}{Nom de la colonne avec le pourcentage d'argile}

\item{grid_m}{Résolution de la grille coarse en mètres (défaut: 20)}

\item{sigma_voisin}{Écart-type du noyau gaussien pour le voisinage en mètres (défaut: 60)}

\item{alpha_voisin}{Pondération de la similarité de voisinage (défaut: 8)}

\item{beta_mnt}{Pondération de l'effet du MNT sur la finesse (défaut: 0.2)}

\item{lambda_base}{Pondération de la probabilité de base (défaut: 0.25)}

\item{base_eps}{Epsilon pour éviter le log(0) (défaut: 1e-6)}

\item{sim_scale}{Échelle de normalisation: "zscore" ou "none" (défaut: "zscore")}

\item{sim_clip}{Valeur de truncation pour le z-score (défaut: 3)}

\item{tempdir}{Répertoire temporaire pour terra (défaut: NULL)}

\item{write_intermediate}{Écrire les rasters intermédiaires (défaut: TRUE)}

\item{verbose}{Afficher les messages de progression (défaut: TRUE)}

\item{progress}{Afficher la barre de progression (défaut: TRUE)}
}
\value{
Une liste contenant:
\itemize{
  \item `prob`: Raster des probabilités par série (SpatRaster)
  \item `ilr_mean`: Raster des moyennes ILR (2 couches)
  \item `dist`: Raster des distances ILR par série
  \item `id_r`: Raster des identifiants de polygone
  \item `closest_index`: Raster avec l'index de la série assignée
  \item `closest_label`: Raster avec les étiquettes de série
  \item `series`: Vecteur des noms de séries
}
}
\description{
Cette fonction calcule les probabilités d'appartenance aux séries de sol
pour chaque pixel en utilisant une combinaison de:
\itemize{
  \item Texture du sol (proportions sable/limon/argile)
  \item Modèle numérique de terrain (MNT)
  \item Voisinage spatial (lissage)
}
Elle effectue ensuite une assignation par quota optimisée en utilisant
la transformation ILR (Isometric Log-Ratio) pour comparer les compositions.
}
\examples{
\dontrun{
resultat <- proba_et_classement_serie_quota_ilr(
  polygones_sf = champs_sf,
  table_series = table_series,
  mnt = dem_raster
)
}

}
